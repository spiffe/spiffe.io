{{- /* Blog RSS feed. Uses publicUrl for absolute links, since the site is
       built with baseURL "/" and Hugo's default feed would emit relative URLs.
       The feed carries summaries; readers follow the link for the full post. */ -}}
{{- $base := .Site.Params.publicUrl | default "" -}}
{{- $pages := .RegularPages.ByDate.Reverse -}}
{{- $limit := .Site.Config.Services.RSS.Limit -}}
{{- if ge $limit 1 -}}{{ $pages = $pages | first $limit }}{{- end -}}
<rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom">
  <channel>
    <title>{{ .Site.Title }} Blog</title>
    <link>{{ printf "%s%s" $base .RelPermalink }}</link>
    <description>{{ with .Description }}{{ . }}{{ else }}{{ .Site.Params.description }}{{ end }}</description>
    <language>{{ .Site.LanguageCode }}</language>
    {{- with (index $pages 0) }}
    <lastBuildDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</lastBuildDate>
    {{- end }}
    <atom:link href="{{ printf "%s%s" $base .RelPermalink }}" rel="self" type="application/rss+xml" />
    {{- range $pages }}
    <item>
      <title>{{ .Title }}</title>
      <link>{{ printf "%s%s" $base .RelPermalink }}</link>
      <pubDate>{{ .Date.Format "Mon, 02 Jan 2006 15:04:05 -0700" | safeHTML }}</pubDate>
      {{- with .Params.author }}<author>{{ . }}</author>{{ end }}
      <guid>{{ printf "%s%s" $base .RelPermalink }}</guid>
      <description>{{ with .Description }}{{ . | html }}{{ else }}{{ .Summary | html }}{{ end }}</description>
    </item>
    {{- end }}
  </channel>
</rss>
