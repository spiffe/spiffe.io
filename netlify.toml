[build]
publish = "public"
command = "pip install -r requirements.txt && make production-build"

[build.environment]
HUGO_VERSION = "0.163.3"
NODE_VERSION = "22"

[context.deploy-preview]
command = "pip install -r requirements.txt && make preview-build"

[context.branch-deploy]
command = "pip install -r requirements.txt && make preview-build"